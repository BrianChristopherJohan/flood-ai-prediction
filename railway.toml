[build]
builder = "DOCKERFILE"

[deploy]
# Model training runs during Docker build (~2 min); give Railway time to pull and deploy.
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
